import React from 'react';

// NOTE: Route name [id] will catch any identifier but won't catch /properties/[id]/lorem. To have it catch any subroute, name the folder [...id]. That way it will serve anything under /[id]/*.

const PropertyAddPage = () => {
  return <div>PropertyAddPage</div>;
};

export default PropertyAddPage;
